// Database variables
const clusterName = "avba-cluster.lyattit.mongodb.net";
const username = "INSERT-USERNAME";
const password = "INSERT-PASSWORD";

// Encryption variables
const secretKey = "INSERT-KEY";

module.exports = {
    clusterName,
    username,
    password,

    secretKey,
};
